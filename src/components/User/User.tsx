// import React from 'react';//1

// type Props = {
//   name: string;
//   age: number;
//   children: React.ReactNode;//React.ReactNode: це більш загальний тип, який містить в собі майже всі можливі типи, які можуть бути відрендерені в компоненті React.
//   // Це може містити string, number, null, undefined, boolean, ReactElement, ReactFragment, ReactPortal.
// };

// export function User({ name, age, children }: Props) {
//   return (
//     <div>
//       <p>{`User name is ${name}`}</p>
//       <p>{`User age is ${age}`}</p>
//       {children}
//     </div>
//   );
// }

